<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card">
      <div class="card-header"><h6 class="mb-0"><i class="bi bi-pencil"></i> Edit Tiket <%= ticket.no_tiket %></h6></div>
      <div class="card-body">
        <form method="POST" action="/tickets/<%= ticket.id %>/update" enctype="multipart/form-data" onsubmit="Modal3D.showLoading('Menyimpan perubahan...')">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <div class="mb-3">
            <label class="form-label">Nama Perangkat <span class="text-danger">*</span></label>
            <input type="text" name="subjek" class="form-control" maxlength="200" value="<%= ticket.subjek %>" required>
          </div>
          <div class="mb-3">
            <label class="form-label">Deskripsi Kerusakan <span class="text-danger">*</span></label>
            <textarea name="deskripsi" class="form-control" rows="4" required><%= ticket.deskripsi || '' %></textarea>
          </div>
          <div class="row mb-3">
            <div class="col-md-4">
              <label class="form-label">Unit / Ruangan</label>
              <input type="text" class="form-control" value="<%= ticket.unit ? ticket.unit.nama_unit : '-' %>" readonly>
            </div>
            <div class="col-md-4">
              <label class="form-label">Layanan <span class="text-danger">*</span></label>
              <select id="selectLayanan" class="form-select" required>
                <option value="">Pilih Layanan</option>
                <% layanans.forEach(function(l) { %>
                <option value="<%= l.id %>"><%= l.nama_layanan %></option>
                <% }) %>
              </select>
            </div>
            <div class="col-md-4">
              <label class="form-label">Kategori <span class="text-danger">*</span></label>
              <select id="selectKategori" name="id_kategori" class="form-select" required>
                <option value="">Pilih Kategori</option>
              </select>
              <input type="hidden" name="kategori" id="hiddenKategori" value="<%= ticket.kategori %>">
            </div>
          </div>
          <div class="row mb-3">
            <div class="col-md-6">
              <label class="form-label">Prioritas</label>
              <select name="prioritas" class="form-select">
                <option value="rendah" <%= ticket.prioritas === 'rendah' ? 'selected' : '' %>>Rendah</option>
                <option value="sedang" <%= ticket.prioritas === 'sedang' ? 'selected' : '' %>>Sedang</option>
                <option value="tinggi" <%= ticket.prioritas === 'tinggi' ? 'selected' : '' %>>Tinggi</option>
                <option value="kritis" <%= ticket.prioritas === 'kritis' ? 'selected' : '' %>>Kritis</option>
              </select>
            </div>
            <div class="col-md-6">
              <label class="form-label">Foto Bukti (opsional)</label>
              <input type="file" name="foto" class="form-control" accept="image/jpeg,jpg,png,gif,webp">
              <% if (ticket.foto) { %>
              <small class="text-muted">Foto saat ini: <%= ticket.foto %></small>
              <% } %>
            </div>
          </div>
          <button type="submit" class="btn btn-primary btn-3d"><i class="bi bi-save"></i> Simpan Perubahan</button>
          <a href="/tickets" class="btn btn-secondary btn-3d"><i class="bi bi-arrow-left"></i> Kembali</a>
        </form>
      </div>
    </div>
  </div>
</div>

<script>
var allKategoris = <%- JSON.stringify(allKategoris) %>;
var selectLayanan = document.getElementById('selectLayanan');
var selectKategori = document.getElementById('selectKategori');
var hiddenKategori = document.getElementById('hiddenKategori');

function loadKategoris(layananId) {
  selectKategori.innerHTML = '<option value="">Pilih Kategori</option>';
  if (!layananId) return;
  allKategoris.forEach(function(k) {
    if (k.id_layanan === layananId) {
      var opt = document.createElement('option');
      opt.value = k.id;
      opt.textContent = k.nama_kategori;
      selectKategori.appendChild(opt);
    }
  });
}

selectLayanan.addEventListener('change', function() {
  loadKategoris(parseInt(this.value));
});

selectKategori.addEventListener('change', function() {
  var selected = this.options[this.selectedIndex];
  hiddenKategori.value = selected ? selected.textContent.toLowerCase().replace(/[^a-z]/g, '') : 'lainnya';
});
</script>
